{
  "name": "@0x0-gen/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@0x0-gen/contracts": "workspace:*",
    "@0x0-gen/logger": "workspace:*",
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.1.8",
    "ws": "^8.18.0"
  }
}
